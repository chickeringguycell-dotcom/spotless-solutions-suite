# Final Pretraining Decision

## Supported Evidence Conclusion
Based on the comprehensive 10-phase Truth Audit spanning strict environmental execution, explicit dataset-license tracing, tensor mapping, and mathematical identity calibration, the final decision is:

**G. No suitable identity engine exists and proprietary training is required.**

### Irrefutable Disproof of Alternatives:
- **A. (Drop-in commercial engine exists):** Disproven. The expanded search (DINO, CLIP, MediaPipe) confirms that no existing open-source model matches the 512-dim face-specific manifold of InsightFace, and models that do (FaceNet) are strictly blocked by their VGGFace2 dataset provenance.
- **B & C. (Adapter retraining for commercial engine):** Disproven. Since FaceNet is legally blocked by its dataset, there is no viable commercial embedding to train an adapter *for*.
- **D. (Valid engine exists, fails cross-view):** Disproven. The only fully permissive engine (Dlib CC0) fails cross-view separation mathematically, but even if it succeeded, PhotoMaker's interface strictly requires an adapter to ingest its 128-dim tensor.
- **E & F. (Geometry is blocker / PanoHead supplies novel views):** Disproven. PanoHead fails on Windows extensions and exceeds 8GB VRAM limits (HARDWARE_BLOCKED). DECA/3DDFA_V2 operate successfully but are legally blocked (BFM/FLAME restrictions). PRNet operates but fails geometrically on profiles.

### Conclusion
The requirement is absolute: Viper Studios must initiate a proprietary training program to construct a commercially safe, synthetic-trained identity embedding network. 
Once trained, the pipeline will also require training a matching MLP Adapter (PhotoMaker layer) to project the new embeddings into the generative cross-attention space.
